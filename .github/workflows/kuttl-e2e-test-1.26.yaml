# This file was auto-generated by hack/generate-tests.sh
name: kuttl e2e test 1.26

on: [push]

jobs:
  test:
    name: kuttl e2e test 1.26
    runs-on: ubuntu-latest
    env:
      KUTTL: /usr/local/bin/kubectl-kuttl
      KUBECTL_STORAGEOS: /usr/local/bin/kubectl-storageos
    steps:
      - name: Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.9.1
        with:
          access_token: ${{ github.token }}
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: '1.18'
      - name: Install dependencies
        run: |
          sudo curl -Lo $KUTTL https://github.com/kudobuilder/kuttl/releases/download/v0.13.0/kubectl-kuttl_0.13.0_linux_x86_64
          sudo chmod +x $KUTTL
          sudo curl -Lo kind https://github.com/kubernetes-sigs/kind/releases/download/v0.15.0/kind-linux-amd64
          sudo chmod +x kind
      - name: Start kind
        run: kind create cluster --retain --wait 2m --config e2e/kind/kind-config-1.26.yaml
      - name: Install kubectl-storageos
        run: |
          make _build
          sudo cp bin/kubectl-storageos $KUBECTL_STORAGEOS
      - name: Run kuttl installer 1.26
        run: kubectl-kuttl test --config e2e/kuttl/kubectl-storageos-installer-1.26.yaml
      - name: Stop kind
        run: kind delete cluster
      - name: Start kind
        run: kind create cluster --retain --wait 2m --config e2e/kind/kind-config-1.26.yaml
      - name: Run kuttl upgrade 1.26
        run: kubectl-kuttl test --config e2e/kuttl/kubectl-storageos-upgrade-1.26.yaml

      - uses: actions/upload-artifact@v3
        if: ${{ always() }} 
        with:
          name: kind-logs
          path: kind-logs-*
